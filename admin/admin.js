(() => {
  const $ = (id) => document.getElementById(id);
  const tokenInput=$('token'), connect=$('connect'), loginStatus=$('loginStatus'), loginCard=$('loginCard'), dashboard=$('dashboard'), list=$('list'), search=$('search'), statusFilter=$('statusFilter'), priorityFilter=$('priorityFilter'), refresh=$('refresh'), logout=$('logout');
  let token='', timer=null, cached=[]; const proposalModal=$('proposalModal'), proposalBody=$('proposalBody'), proposalTitle=$('proposalTitle');
  const statuses=['new','contacted','replied','discovery','proposal','won','lost'];
  const labels={new:'New',contacted:'Contacted',replied:'Replied',discovery:'Discovery',proposal:'Proposal',won:'Won',lost:'Lost'};
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const fmt=v=>v?new Date(v).toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'}):'—';
  const isoDate=d=>d?new Date(d):null;
  function priority(lead){
    if (lead.status==='won'||lead.status==='lost') return lead.status==='won'?'won':'closed';
    let s=Number(lead.score||0);
    if (String(lead.signal||'').toLowerCase().includes('high')) s+=15;
    if (lead.status==='proposal') s+=12; else if (lead.status==='discovery') s+=8; else if (lead.status==='replied') s+=5;
    const next=isoDate(lead.next_action_at); const now=new Date();
    if(next){ if(next < now) s+=25; else if(next-now < 48*3600*1000) s+=10; }
    if(s>=75) return 'hot'; if(s>=45) return 'warm'; return 'cold';
  }
  function actionLabel(lead){
    if(lead.status==='won') return 'Won';
    if(lead.status==='lost') return 'Closed';
    if(!lead.next_action_at) return 'Set next action';
    const next=new Date(lead.next_action_at), now=new Date();
    if(next<now) return 'Overdue';
    const day=new Date(now); day.setHours(23,59,59,999);
    if(next<=day) return 'Today';
    return fmt(next);
  }
  const blueprint=raw=>{try{const xs=JSON.parse(raw||'[]');return xs.length?`<ol>${xs.slice(0,6).map(x=>`<li><b>${esc(x.title)}</b><span>${esc(x.detail)}</span></li>`).join('')}</ol>`:'<span class="muted">No blueprint</span>'}catch{return '<span class="muted">Invalid blueprint</span>'}};
  const statusSelect=(lead)=>`<select class="stage" data-id="${esc(lead.id)}">${statuses.map(s=>`<option value="${s}" ${s===lead.status?'selected':''}>${labels[s]}</option>`).join('')}</select>`;
  async function api(url,opts={}){const r=await fetch(url,{...opts,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json',...(opts.headers||{})},cache:'no-store'});const d=await r.json().catch(()=>({}));if(r.status===401)throw new Error('Invalid Admin Token.');if(!r.ok||!d.ok)throw new Error(d.error||'Request failed.');return d;}
  async function updateLead(id,patch){return api(`/api/admin/leads?id=${encodeURIComponent(id)}`,{method:'PATCH',body:JSON.stringify(patch)})}
  function renderPipeline(counts){$('pipeline').innerHTML=statuses.map(s=>`<button type="button" class="pipe ${s===statusFilter.value?'active':''}" data-status="${s}"><span>${labels[s]}</span><strong>${counts[s]||0}</strong></button>`).join('')+'<div class="pipe total"><span>Total</span><strong>'+Object.values(counts).reduce((a,b)=>a+b,0)+'</strong></div>';document.querySelectorAll('.pipe[data-status]').forEach(b=>b.onclick=()=>{statusFilter.value=b.dataset.status;load()});}
  function renderPriority(counts){$('priority').innerHTML=['hot','warm','cold'].map(s=>`<button type="button" class="priority-card ${s===priorityFilter.value?'active':''}" data-priority="${s}"><span>${s.toUpperCase()}</span><strong>${counts[s]||0}</strong></button>`).join('');document.querySelectorAll('.priority-card').forEach(b=>b.onclick=()=>{priorityFilter.value=b.dataset.priority;renderList();});}
  function scopeItems(raw){try{const xs=JSON.parse(raw||'[]');return Array.isArray(xs)?xs:[]}catch{return []}}
  function proposalView(p){
    const items=scopeItems(p.scope_json);
    return `<div class="proposal-meta"><span class="badge ${esc(p.status)}">${esc(p.status)}</span><span>${esc(p.timeline)}</span><strong>€${Number(p.price_eur||0).toLocaleString('de-DE')}</strong></div><p>${esc(p.executive_summary)}</p><h3>Scope</h3><ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><div class="proposal-actions"><label>Title<input id="editPropTitle" value="${esc(p.title)}"></label><label>Timeline<input id="editPropTimeline" value="${esc(p.timeline)}"></label><label>Price (€)<input id="editPropPrice" type="number" min="0" step="500" value="${Number(p.price_eur||0)}"></label><label>Summary<textarea id="editPropSummary" rows="5">${esc(p.executive_summary)}</textarea></label><div class="actions"><button id="saveProposal" type="button">Save proposal</button><button id="copyProposal" type="button" class="ghost">Copy text</button><button id="clientLink" type="button" class="ghost">Create client link</button></div></div>`;
  }
  async function openProposal(leadId){
    proposalModal.classList.remove('hidden'); proposalBody.innerHTML='<div class="panel empty">Generating proposal…</div>';
    try{
      const existing=await api(`/api/admin/proposals?lead_id=${encodeURIComponent(leadId)}`);
      let p=existing.proposals?.[0];
      if(!p){ const created=await api('/api/admin/proposals',{method:'POST',body:JSON.stringify({lead_id:leadId})}); p=created.proposal; }
      proposalTitle.textContent=p.title; proposalBody.innerHTML=proposalView(p);
      $('saveProposal').onclick=async()=>{try{await api(`/api/admin/proposals?id=${encodeURIComponent(p.id)}`,{method:'PATCH',body:JSON.stringify({title:$('editPropTitle').value, timeline:$('editPropTimeline').value, price_eur:Number($('editPropPrice').value||0), executive_summary:$('editPropSummary').value})}); alert('Proposal saved.'); openProposal(leadId)}catch(e){alert(e.message)}};
      $('clientLink').onclick=async()=>{try{const r=await api('/api/admin/portal-link',{method:'POST',body:JSON.stringify({proposal_id:p.id})});await navigator.clipboard.writeText(r.client_url);$('clientLink').textContent='Link copied';setTimeout(()=>$('clientLink').textContent='Create client link',1200)}catch(e){alert(e.message)}};
      $('copyProposal').onclick=async()=>{const text=`${$('editPropTitle').value}\n\n${$('editPropSummary').value}\n\nScope:\n${scopeItems(p.scope_json).map(x=>'- '+x).join('\n')}\n\nTimeline: ${$('editPropTimeline').value}\nPrice: €${Number($('editPropPrice').value||0).toLocaleString('de-DE')}`; try{await navigator.clipboard.writeText(text); $('copyProposal').textContent='Copied';}catch{alert(text)}};
    }catch(e){proposalBody.innerHTML=`<div class="panel empty">${esc(e.message)}</div>`}
  }
  function renderList(){
    let leads=cached.slice();
    const pf=priorityFilter.value; if(pf) leads=leads.filter(l=>priority(l)===pf);
    const q=search.value.trim().toLowerCase(); if(q) leads=leads.filter(l=>[l.name,l.email,l.company,l.focus,l.workflow].some(v=>String(v||'').toLowerCase().includes(q)));
    if(!leads.length){list.innerHTML='<div class="panel empty">No leads found.</div>';return;}
    list.innerHTML=leads.map(l=>{const p=priority(l); return `<article class="lead panel"><div class="lead-head"><div><p class="eyebrow">${esc(l.source||'lead')} · ${p.toUpperCase()}</p><h2>${esc(l.company)}</h2><p class="person">${esc(l.name)} · ${esc(l.email)}</p></div><div>${statusSelect(l)}<div class="date">${fmt(l.created_at)}</div></div></div><div class="lead-grid"><div><span class="label">Priority</span><p><strong class="badge ${p}">${p.toUpperCase()}</strong> · score ${esc(l.score??'—')}/100</p></div><div><span class="label">Workflow</span><p>${esc(l.workflow||l.focus||'—')}</p></div><div><span class="label">Next action</span><input class="next-action" data-id="${esc(l.id)}" type="datetime-local" value="${l.next_action_at?new Date(l.next_action_at).toISOString().slice(0,16):''}"><small class="action-state">${esc(actionLabel(l))}</small></div></div><div class="notes"><span class="label">Notes</span><textarea class="note" data-id="${esc(l.id)}" rows="3" placeholder="Discovery notes, objections, next steps…">${esc(l.notes)}</textarea><div class="actions"><button type="button" class="save-note" data-id="${esc(l.id)}">Save notes</button><button type="button" class="mark-contacted ghost" data-id="${esc(l.id)}">Mark contacted</button><button type="button" class="generate-proposal" data-id="${esc(l.id)}">Generate proposal</button></div></div><details><summary>Suggested blueprint</summary>${blueprint(l.blueprint_json)}</details></article>`;}).join('');
    document.querySelectorAll('.stage').forEach(el=>el.onchange=async()=>{try{await updateLead(el.dataset.id,{status:el.value});await load()}catch(e){alert(e.message)}});
    document.querySelectorAll('.save-note').forEach(b=>b.onclick=async()=>{const note=document.querySelector(`.note[data-id="${CSS.escape(b.dataset.id)}"]`).value;const input=document.querySelector(`.next-action[data-id="${CSS.escape(b.dataset.id)}"]`).value;try{await updateLead(b.dataset.id,{notes:note,next_action_at:input?new Date(input).toISOString():null});b.textContent='Saved';setTimeout(()=>b.textContent='Save notes',900);await load()}catch(e){alert(e.message)}});
    document.querySelectorAll('.next-action').forEach(el=>el.onchange=async()=>{try{await updateLead(el.dataset.id,{next_action_at:el.value?new Date(el.value).toISOString():null});await load()}catch(e){alert(e.message)}});
    document.querySelectorAll('.mark-contacted').forEach(b=>b.onclick=async()=>{try{await updateLead(b.dataset.id,{status:'contacted',contacted:true});await load()}catch(e){alert(e.message)}});
    document.querySelectorAll('.generate-proposal').forEach(b=>b.onclick=()=>openProposal(b.dataset.id));
  }
  function renderProjects(projects){const el=$('projects');if(!el)return;if(!projects.length){el.innerHTML='<div class="muted">No activated projects yet.</div>';return;}el.innerHTML=projects.map(p=>{const total=Number(p.task_total||0),done=Number(p.task_done||0),pct=total?Math.round(done*100/total):0;return `<article class="project-card"><div class="project-main"><p class="eyebrow">${esc(p.status||'onboarding')} · ${esc(p.environment||'test')}</p><h3>${esc(p.company)}</h3><p>${esc(p.name||p.contract_title||'NEREON Project')} · ${esc(p.lead_name||'')}</p><div class="progress"><span style="width:${pct}%"></span></div><small class="muted">Onboarding ${done}/${total} · ${pct}%</small></div><div class="project-side"><strong>€${Number(p.amount_eur||0).toLocaleString('de-DE')}</strong><div class="muted">Invoice: ${esc(p.invoice_status||'—')}</div><select class="project-status" data-id="${esc(p.id)}"><option value="onboarding" ${p.status==='onboarding'?'selected':''}>Onboarding</option><option value="active" ${p.status==='active'?'selected':''}>Active</option><option value="blocked" ${p.status==='blocked'?'selected':''}>Blocked</option><option value="complete" ${p.status==='complete'?'selected':''}>Complete</option><option value="archived" ${p.status==='archived'?'selected':''}>Archived</option></select><button type="button" class="ghost view-project" data-id="${esc(p.id)}">Tasks</button></div></article>`}).join('');document.querySelectorAll('.project-status').forEach(el=>el.onchange=async()=>{try{await api(`/api/admin/projects?id=${encodeURIComponent(el.dataset.id)}`,{method:'PATCH',body:JSON.stringify({status:el.value})});await load()}catch(e){alert(e.message)}});document.querySelectorAll('.view-project').forEach(b=>b.onclick=()=>openProjectTasks(b.dataset.id));}
  async function openProjectTasks(projectId){const win=window.open('about:blank','_blank','noopener');try{const d=await api(`/api/admin/projects?id=${encodeURIComponent(projectId)}`);const p=d.project;const html=(d.tasks||[]).map(t=>`<div style="padding:14px;border-bottom:1px solid #22313b"><b>${esc(t.title)}</b><p style="opacity:.72">${esc(t.detail||'')}</p><select data-task="${esc(t.id)}"><option value="todo" ${t.status==='todo'?'selected':''}>To do</option><option value="in_progress" ${t.status==='in_progress'?'selected':''}>In progress</option><option value="blocked" ${t.status==='blocked'?'selected':''}>Blocked</option><option value="done" ${t.status==='done'?'selected':''}>Done</option></select></div>`).join('');win.document.write(`<title>NEREON Project Workspace</title><body style="font-family:system-ui;background:#071017;color:#eef8ff;padding:32px;max-width:980px;margin:auto"><div style="opacity:.65;font-size:12px;letter-spacing:.12em">NEREON PROJECT WORKSPACE</div><h1>${esc(p.name||p.company)}</h1><p>${esc(p.company||'')} · ${esc(p.status||'')}</p><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin:20px 0"><div style="padding:16px;background:#101b22;border:1px solid #22313b;border-radius:14px"><small>Value</small><strong style="display:block;font-size:24px">€${Number(p.amount_eur||0).toLocaleString('de-DE')}</strong></div><div style="padding:16px;background:#101b22;border:1px solid #22313b;border-radius:14px"><small>Invoice</small><strong style="display:block;font-size:24px">${esc(p.invoice_status||'—')}</strong></div><div style="padding:16px;background:#101b22;border:1px solid #22313b;border-radius:14px"><small>Client</small><strong style="display:block;font-size:18px">${esc(p.lead_name||p.name||'—')}</strong></div></div><h2>Tasks</h2><div id="tasks">${html||'<p>No tasks yet.</p>'}</div><h2 style="margin-top:28px">Add task</h2><input id="title" placeholder="Task title" style="display:block;width:100%;padding:12px;margin-bottom:8px"><input id="detail" placeholder="Details (optional)" style="display:block;width:100%;padding:12px;margin-bottom:8px"><button id="add">Add task</button><script>const tok=localStorage.getItem('nereon_admin_token')||'';const headers={'Content-Type':'application/json','Authorization':'Bearer '+tok};document.querySelectorAll('select[data-task]').forEach(s=>s.onchange=async()=>{await fetch('/api/admin/onboarding-tasks?id='+encodeURIComponent(s.dataset.task),{method:'PATCH',headers,body:JSON.stringify({status:s.value})});location.reload()});document.getElementById('add').onclick=async()=>{const title=document.getElementById('title').value.trim();const detail=document.getElementById('detail').value; if(!title)return; const r=await fetch('/api/admin/onboarding-tasks',{method:'POST',headers,body:JSON.stringify({project_id:${JSON.stringify(projectId)},title,detail})}); if(!r.ok){alert(await r.text());return;} location.reload();}</script></body>`);win.document.close();}catch(e){win.close();alert(e.message)}}
  async function load(){list.innerHTML='<div class="panel empty">Loading leads…</div>';try{const d=await api(`/api/admin/leads?limit=200`);cached=d.leads.map(x=>({...x,status:x.status||'new'}));
      const filtered=cached.filter(l=>{const q=search.value.trim().toLowerCase();return !q||[l.name,l.email,l.company,l.focus,l.workflow].some(v=>String(v||'').toLowerCase().includes(q))}).filter(l=>!statusFilter.value||l.status===statusFilter.value);
      $('count').textContent=cached.length; $('high').textContent=cached.filter(x=>priority(x)==='hot').length; $('won').textContent=d.counts.won||0;
      const actionable=cached.filter(x=>x.next_action_at&&x.status!=='won'&&x.status!=='lost');
      const today=new Date(); today.setHours(23,59,59,999); const start=new Date(); start.setHours(0,0,0,0);
      const overdue=actionable.filter(x=>new Date(x.next_action_at)<start).length; const todayCount=actionable.filter(x=>{const t=new Date(x.next_action_at);return t>=start&&t<=today}).length;
      $('next').textContent=overdue?`${overdue} overdue`:todayCount?`${todayCount} today`:'—';
      const pc={hot:0,warm:0,cold:0};cached.forEach(l=>{const p=priority(l);if(pc[p]!=null)pc[p]++});renderPipeline(d.counts);renderPriority(pc);renderList();
      $('overdue').textContent=overdue; $('today').textContent=todayCount;
      try{const projects=await api('/api/admin/projects');renderProjects(projects.projects||[]);}catch(err){renderProjects([]);}
    }catch(e){loginStatus.textContent=e.message;loginStatus.className='status error';if(e.message.includes('Invalid Admin Token'))lock()}}
  async function connectAdmin(){const c=tokenInput.value.trim();if(!c){loginStatus.textContent='Enter the Admin Token.';loginStatus.className='status error';return}token=c;try{localStorage.setItem('nereon_admin_token',`Bearer ${c}`)}catch{};loginStatus.textContent='Connecting…';loginStatus.className='status';try{await api('/api/admin/leads?limit=1');loginCard.classList.add('hidden');dashboard.classList.remove('hidden');await load()}catch(e){token='';loginStatus.textContent=e.message;loginStatus.className='status error'}}
  function lock(){try{localStorage.removeItem('nereon_admin_token')}catch{};token='';loginCard.classList.remove('hidden');dashboard.classList.add('hidden');tokenInput.value='';if(timer)clearTimeout(timer)}
  $('closeProposal').onclick=()=>proposalModal.classList.add('hidden'); proposalModal.addEventListener('click',e=>{if(e.target===proposalModal) proposalModal.classList.add('hidden')}); connect.onclick=connectAdmin;tokenInput.onkeydown=e=>{if(e.key==='Enter')connectAdmin()};refresh.onclick=load;const rp=$('refreshProjects');if(rp)rp.onclick=load;logout.onclick=lock;statusFilter.onchange=load;priorityFilter.onchange=renderList;search.oninput=()=>{clearTimeout(timer);timer=setTimeout(renderList,150)};
})();
