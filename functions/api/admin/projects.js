function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})}
function authorized(request,env){const e=String(env?.ADMIN_TOKEN||'');const h=request.headers.get('Authorization')||'';return !!e&&h.startsWith('Bearer ')&&h.slice(7)===e}
export async function onRequestGet({request,env}){if(!authorized(request,env))return json({ok:false,error:'unauthorized'},401);if(!env?.DB)return json({ok:false,error:'database_not_configured'},503);try{const r=await env.DB.prepare(`SELECT p.*,l.name lead_name,l.company,l.email,c.title contract_title,i.id invoice_id,i.status invoice_status FROM projects p JOIN leads l ON l.id=p.lead_id JOIN contracts c ON c.id=p.contract_id LEFT JOIN invoices i ON i.contract_id=c.id ORDER BY p.created_at DESC LIMIT 200`).all();return json({ok:true,projects:r.results})}catch(e){console.error(e);return json({ok:false,error:'database_read_failed'},500)}}
